package com.example.interviewproject.data.repository

import javax.inject.Inject
/*
class InterviewRepositoryImpl@Inject constructor(private val api:):InterviewRepositoryRepository{
    override suspend fun getRequest(LoginString: String): ButtonDto {
        return .getRequest(LoginString = LoginString)
    }
}
*/