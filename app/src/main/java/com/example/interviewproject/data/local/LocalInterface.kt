package com.example.interviewproject.data.local

import retrofit2.http.GET
import retrofit2.http.Query
/*
interface LocalInterface {
    @GET(".")
    suspend fun sendRequest(
        @Query("") LoginString: String,
        @Query("IP") IP:String = IP
    ) : ButtonDto
    }
        */